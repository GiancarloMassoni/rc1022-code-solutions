/* exported reverse */
// Gather all the indexes of the array and place them into a new array in reverse
function reverse(array) {
  var newArray = [];
  for (var i = array.length - 1; i >= 0; i--) {
    newArray.push(array[i]);
  }
  return newArray;
}
